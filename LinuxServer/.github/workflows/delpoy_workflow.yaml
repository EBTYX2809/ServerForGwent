name: Deploy server

on:
  push:
    branches: -master

jobs:
  cleanup_old_container:
    runs-on: self-hosted
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Stop old container
        run: docker stop server_for_gwent

      - name: Remove old container
        run: docker rm server_for_gwent

      - name: Cleanup containers
        run: docker system prune -f

  build_new_container:
    runs-on: self-hosted
    steps:
      - name: Build container
        run: docker build -t server_for_gwent

  run_new_container:
    runs-on: self-hosted
    steps:
      - name: Run container
        run: docker run -d -p 10000:10000 --name server_for_gwent server_for_gwent
